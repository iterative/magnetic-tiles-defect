{
    "name": "magnetic-tiles-defect",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "extensions": ["Iterative.dvc"],
    "postCreateCommand": "sudo apt update; sudo apt install -y nvtop; pip install --user pipenv; pipenv install; pipenv shell"
  }