{
    "name": "magnetic-tiles-defect",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "extensions": ["Iterative.dvc"],
    "postCreateCommand": "sudo apt update; sudo install -y nvtop; pip install --user pipenv; pipenv install; pipenv shell"
  }