{
    "name": "magnetic-tiles-defect",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "extensions": ["Iterative.dvc"],
    "postCreateCommand": "apt install -y nvtop; pip install --user pipenv; pipenv install"
  }